#!/bin/bash
#command line arguments

name=${1}
age=${2}

echo "my name is ${name} and my age is ${age}"
 # when we are entering a command in terminal for eg= ./command_line.sh saalim 23 (afterspace we have to give value)